{
  "name": "opencode-provider-proxy",
  "version": "1.0.0",
  "private": true,
  "description": "Per-provider proxy configuration plugin for OpenCode",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "prepare": "npm run build",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "http-proxy-agent": "^9.1.0",
    "https-proxy-agent": "^9.1.0",
    "socks-proxy-agent": "^9.0.0"
  },
  "devDependencies": {
    "@opencode-ai/plugin": "^1.14.25",
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
